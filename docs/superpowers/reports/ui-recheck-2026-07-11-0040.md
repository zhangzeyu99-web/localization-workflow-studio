# 新版工作台 UI 复检记录（2026-07-11 00:40，branch codex/frontend-f5-sol-ux @ 77191ea）

## 检查方式

- 真实浏览器（Playwright CLI）访问 http://127.0.0.1:5173，只读点击，不改数据。
- 1440x900 与 1125x900 两档宽度；截图存于 `.playwright-cli/`。
- 同时跑了前端 build 和全量 e2e。

## 结论：整体通过

| 场景 | 结果 |
| --- | --- |
| 项目总览（1440px） | 通过。浅色中性风格 + 青色主色，无旧紫色渐变残留，无重叠/截断 |
| 项目页（Repro Manual Fix） | 通过。统计卡、后台任务横幅、六个标签页渲染正常 |
| 九步向导第 1 步 | 通过。已重组为 4 阶段（准备/输入/处理/交付）+ 步骤 1/9 下拉 |
| 向导第 7 步 AI 翻译 | 通过。「深度校对」勾选项存在，右侧「本次输入」含历史译文条数 |
| 向导第 8 步 QA 校对 | 通过。右侧仅一个 QA 结论区（QA 未通过 + 修复动作组） |
| 1125px 窄桌面 | 通过。scrollWidth == clientWidth（1125），无横向溢出、右栏不挤压 |
| 浏览器控制台 | 无 ERROR/WARN（仅 React DevTools 提示） |
| 前端 build | 成功（6.78s） |
| e2e 全量 | 37/37 通过（3.6m），含新增归档引用/逐行校对证据、紧凑宽度用例 |

## 发现的问题（已于 2026-07-11 01:00 修复）

1. **QA 问题摘要裸露内部规则码**（已修复）：第 8 步「QA 问题摘要」中显示英文标签 `forbidden_translation: 1`。修复：`QaIssuePanel.tsx` 的 `issueTypeLabel` 补齐后端全部规则码中文映射（禁用译法、固定译名未使用、项目必须规则、项目规则配置无效、QA 环境异常、行序不一致、译文为空、译文残留中文、保护标记丢失）。真实页面读回确认 chip 显示「禁用译法: 1」。
2. **首屏短暂显示空态**（已修复）：项目列表请求返回前，主区先渲染「还没有项目」空态。修复：`main.tsx` 增加 `projectsReady` 状态，首次拉取完成前 `EmptyState` 渲染「正在加载项目…」占位（`data-testid="project-list-loading"`），加载完成后正常切换。
3. **提示词面板含 `term_hits` 字段名**：项目翻译提示词正文出现内部字段名 `term_hits`。这是真实 AI 提示词内容，属预期，仅备查。
4. **qa.py 中文字符串编码确认无损**：终端 diff 显示的乱码为 PowerShell 显示问题，UTF-8 读取验证 `跳过：该工作表没有译文列…` 完整、无 U+FFFD。

## 隔壁 Codex 会话进度（同一时间点）

- 最新提交 77191ea（23:41）：QA 修复跳过无译文列的确认表（后端 qa.py + 单测）、e2e 扩到 37 条、PhaseStepper/StepShell 响应式细化、大量验收截图入库。
- 工作树干净，已与 origin 同步（`## codex/frontend-f5-sol-ux...origin/...`，无 ahead/behind）。
- Computer Use 直连验证仍是遗留项（需要用户直开任务，见 computer-use-direct-validation-handoff-2026-07-10.md）。
