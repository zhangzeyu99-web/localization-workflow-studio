# Localization Workflow Studio Design System

> 状态：前端优化设计基线，2026-07-10。
> 目标：让本地化操作人员在 3 秒内看懂当前状态、下一步和风险，并在不丢失专业能力的前提下减少视觉噪音。
> 参考方向：借鉴 Cal.com 的中性克制与结构精度，但按高频操作工作台重新设计，不复制其营销页大字号和大留白。

## 1. 设计命题

设计主题：**Quiet Localization Operations，安静、可信、可追溯的本地化工作台。**

界面不是品牌展示页，也不是技术日志查看器。它服务三类高频动作：

1. 判断当前项目下一步该做什么。
2. 启动、观察和恢复翻译或 QA 任务。
3. 判断交付是否可信，并下载或继续修复。

所有页面遵守以下优先级：

1. 当前结论。
2. 下一步主操作。
3. 风险与证据。
4. 历史、配置和审计细节。

## 2. 核心原则

- 每个可见区域最多一个主按钮。其余动作降为次按钮、文字按钮或折叠区。
- 状态只出现一次主结论；其他位置只补充证据，不重复结论。
- 颜色表达状态，不表达装饰。品牌框架保持中性，业务状态使用有限语义色。
- 保留完整流程能力，但默认只展开当前任务需要的信息。
- 不把后端枚举、UUID、字段名、文件路径、内部步骤码直接暴露给普通用户。
- 步骤编号只用于导航，正文使用中文任务名称，不反复显示 `STEP 7/8/9`。
- 不使用渐变背景、玻璃拟态、发光、装饰性 emoji、嵌套卡片和无意义统计块。
- 操作工具保持紧凑，但不以截断关键文本换取密度。

## 3. 视觉系统

### 3.1 色彩

第一阶段只做浅色主题。暗色主题不与本轮优化并行，避免双份视觉状态拖慢收口。

| Token | 色值 | 用途 |
| --- | --- | --- |
| `--color-canvas` | `#F6F7F8` | 页面背景 |
| `--color-surface` | `#FFFFFF` | 主工作面、弹窗、表格 |
| `--color-surface-subtle` | `#F0F2F4` | 次级区、选中前状态 |
| `--color-border` | `#D9DEE3` | 常规边界 |
| `--color-border-strong` | `#B9C1C9` | 强调边界、悬停 |
| `--color-text` | `#172026` | 主文本 |
| `--color-text-secondary` | `#596673` | 次文本 |
| `--color-text-muted` | `#7B8792` | 元数据、禁用提示 |
| `--color-accent` | `#0F766E` | 主操作、当前步骤 |
| `--color-accent-hover` | `#115E59` | 主操作悬停 |
| `--color-accent-soft` | `#CCFBF1` | 当前项、轻量选中 |
| `--color-info` | `#2563EB` | 信息与运行中 |
| `--color-success` | `#15803D` | 通过、可信归档 |
| `--color-warning` | `#B45309` | 待复核、带问题交付 |
| `--color-danger` | `#B91C1C` | 阻断、失败、删除 |

语义色必须同时配合文字或图标，不能只靠颜色区分状态。

### 3.2 字体

不引入远程字体，优先保证中文、内网和离线部署稳定。

```css
font-family: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif;
```

| 角色 | 字号 | 字重 | 行高 |
| --- | --- | --- | --- |
| 页面标题 | 24px | 650 | 1.25 |
| 流程标题 | 20px | 650 | 1.3 |
| 区块标题 | 16px | 600 | 1.4 |
| 正文 | 14px | 400 | 1.55 |
| 控件文字 | 14px | 550 | 1.2 |
| 标签与元数据 | 12px | 500 | 1.4 |

所有文字 `letter-spacing: 0`。不按视口宽度缩放字号。

### 3.3 间距与圆角

- 间距：`4 / 8 / 12 / 16 / 24 / 32px`。
- 按钮、输入框：6px 圆角。
- 普通容器、表格、弹窗：8px 圆角。
- 标签仅在状态短词时使用 pill；普通按钮不得做胶囊形。
- 页面区块优先用间距和分隔线组织，不把每个区块都包成卡片。

### 3.4 阴影与层级

- 页面和固定区块默认无阴影。
- 浮层、菜单、弹窗使用一档轻阴影：`0 8px 24px rgba(23, 32, 38, 0.12)`。
- 当前任务面不使用发光或彩色阴影。
- 层级主要由背景、边界、留白和标题建立。

## 4. 布局系统

### 4.1 应用框架

- 顶栏高度 56px，只放产品名、全局任务状态、设置和必要操作。
- 项目侧栏桌面宽度 240px；项目名可换行，关键名称不省略。
- 主工作区宽度自适应，最大内容宽度 1440px。
- 项目概览第一屏只回答：当前最重要任务是什么、下一步点哪里、最近发生了什么。

### 4.2 流程工作区

- 流程保留 9 个真实步骤，但视觉上分成 4 个阶段：准备、输入、处理、交付。
- 当前步骤正文为主列；状态证据和下载为上下文列。
- 当主工作区内容宽度小于 980px 时，上下文列移动到正文下方，不再挤压左右两列。
- 使用容器查询判断工作区宽度，避免只按浏览器宽度判断而忽略项目侧栏。
- 折叠后顺序固定：结论、主操作、风险、证据、历史。

### 4.3 表格

- 表头固定，首个识别列在宽表中可固定。
- 允许横向滚动，但操作列始终可达。
- 100 行分页保留；搜索、语言筛选和分页组成同一工具栏。
- 译文归档必须显示来源可信度，不再只展示译文本身。

## 5. 组件规范

### 5.1 按钮

- 主按钮：实心青绿色，表示当前推荐动作。
- 次按钮：白底边框，用于替代路径。
- 危险按钮：红色，仅用于取消、删除等不可逆动作。
- 图标按钮使用 `lucide-react` 的 16px 图标，并有可见 tooltip 或 `aria-label`。
- 主按钮高度 40px，紧凑按钮 32px，触摸目标不小于 40px。
- 禁用按钮必须解释原因；不能只降低透明度。

### 5.2 状态条

每个任务面只保留一个主状态条，结构固定：

```text
[状态] 一句话结论
补充证据或风险
[主操作] [次操作]
```

状态词统一为：

- 待开始
- 检查中
- 运行中
- 需要处理
- 可进入下一步
- 已通过
- 可带问题交付
- 已交付
- 已取消

### 5.3 步骤导航

- 桌面显示 4 个阶段和当前阶段内步骤。
- 移动端显示当前阶段、当前步骤和前后导航，不铺满 9 个按钮。
- 已跳过步骤显示“已跳过”，不伪装成“已完成”。
- 已译表分流到 QA 时，术语候选和 AI 翻译明确标为跳过。

### 5.4 上传与文件选择

- 已选资产、上传新文件和下载模板属于同一个输入区，不拆成多个并列卡片。
- 错误模板在原位置显示错误原因、正确列要求和下载模板动作。
- 上传成功后显示文件名、识别类型、行数和下一步，不再显示泛化成功提示。

### 5.5 QA 结果

- QA 通过：主操作“生成标准交付”。
- QA 未通过：主操作“修复并重跑 QA”，次操作“生成带问题摘要的交付”。
- 必须修复与建议修复分开计数。
- 问题列表按可编辑、不可编辑、来源和严重度过滤。
- 问题详情显示人话说明，原始规则码放在折叠详情。

### 5.6 译文归档来源

| `source_type` | 用户标签 | 可信度表达 |
| --- | --- | --- |
| `qa_passed` / `qa_final` | QA 已通过 | 可信参考 |
| `manual` | 人工维护 | 人工确认 |
| `imported` / `archive` / `translation_archive` | 外部导入 | 来源待确认 |
| `delivered_with_issues` | 带问题交付 | 待复核，不作为首选 |

筛选默认显示全部；参考匹配优先级以服务端规则为准，界面只解释，不允许前端另造排序语义。

## 6. 内容规范

- 结论先说结果，再说原因，再给动作。
- 操作提示必须回答“发生了什么”和“现在做什么”。
- 同一状态不得在标题、右栏、黄色提示、页头状态条重复四次。
- 正文不使用 `workpack`、`cache-lint`、`reference_audit` 等内部名；审计详情中可显示。
- “QA 必须通过才能交付”改为准确语义：推荐修复到 0；急需时允许带问题摘要交付并标记待复核归档。
- 过程页不显示完整文件路径；下载区显示文件用途和文件名。

## 7. 响应式与可访问性

验收视口：`1440x900`、`1125x903`、`768x1024`、`390x844`。

- 1125px 视口不得继续维持被挤压的双栏；上下文区应自动下移。
- 390px 视口无横向页面滚动；只有表格容器允许横向滚动。
- 键盘可完成项目切换、步骤导航、语言选择、QA 修复和交付生成。
- 焦点环 2px，可见且不被 overflow 裁切。
- 状态变化使用 `aria-live="polite"`，错误使用 `role="alert"`。
- 动画时长 120-180ms；尊重 `prefers-reduced-motion`。

## 8. 禁止项

- 紫蓝粉渐变顶栏和大面积深蓝背景。
- 玻璃卡片、模糊背景、发光描边。
- 一个区块嵌套三层卡片。
- 以 emoji 充当导航、按钮或状态图标。
- 关键事实使用省略号截断。
- 一个屏幕同时出现多个高饱和主按钮。
- 为了“高级感”增加营销文案、大标题或装饰图形。
